export enum Shape {
    Diamond,
    Wave,
    Line
}

export enum Color {
    Red,
    Blue,
    Green
}

export enum Filling {
    Full,
    Empty,
    Striped
}

export enum CardProperty {
    Shape = 'shape',
    Number = 'number',
    Filling = 'filling',
    Color = 'color'
}